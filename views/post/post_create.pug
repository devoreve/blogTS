extends ../layout

block content
    h1 Nouvel article

    if errors && errors.length
        ul
            each error in errors
                li(style="color: #b22222")= error

    form(method="post")
        div
            label(for="title") Titre
            input(id="title" name="title" type="text")
        div
            label(for="content") Contenu
            textarea(id="content" name="content" rows="10")
        div
            button Créer
